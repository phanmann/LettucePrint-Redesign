
## Flushing Cannabis Co. — Waitlist Hotfix
- Fixed live coming-soon waitlist signup failure on Vercel: `/api/waitlist` no longer writes to serverless local filesystem; it now persists to Google Sheet `18f4Dkrv6x7UD5D-oqqZW0kHZFL3gnPQ_mPGK65ouuYA` via `src/lib/waitlistStorage.ts` with `/tmp`/local JSON fallback.
- Added production Vercel env vars `GOOGLE_SERVICE_ACCOUNT_JSON` and `WAITLIST_SPREADSHEET_ID`.
- Minimized stored PII to first name, last name, email, submittedAt, id. Removed IP/user-agent storage.
- Updated privacy disclosures for waitlist collection, Google Sheets storage, opt-in launch/offer emails, and retention; because pre-launch lockdown still redirects `/privacy`, added accessible `/coming-soon/privacy` and linked it from the form.
- Verified production POST to `https://www.flushingcannabis.com/api/waitlist` returns 201 and appends to the Google Sheet; cleaned all Athena test rows afterward.
- Rotated Flushing admin password for `wei@mrtalaw.com` to Steven-requested value; deployed to production and verified login/session/admin data access. Current submissions count after test cleanup: 0.
- Updated Flushing `/admin` for accessibility/legibility: larger type, darker text, stronger borders/focus rings, table `scope=col`, labeled search control, horizontal table scroll, improved empty/loading states, and CSV export sanitization against formula injection. Built + deployed to production; verified admin login and data endpoint.

## Flushing Cannabis Co. — Carrot/Cova Go-Live Info Received
- Carrot team confirmed they received the Cova API and completed setup on their side.
- Carrot embed credentials received: Space Key `sp_oCAJfrv5CxoviuditKC8X`, Location ID `1`, Company Key `flushing`, admin URL `staff.getcarrot.io`, admin email `wei@mrtalaw.com`.
- Carrot next steps: configure categories once inventory starts arriving; website team must embed Carrot Store, ensure HTML routing, create kiosk page, then send Carrot a test site for review.
- Store opening next week: treat Carrot menu/store go-live as urgent; prior blocker “Carrot Space Key + Location ID” is resolved.

## Flushing Cannabis Co. — Carrot Store/Kiosk Review Flow Live
- Wired Carrot production config for Flushing: Space Key `sp_oCAJfrv5CxoviuditKC8X`, Location ID `1`, API `https://api.nevada.getcarrot.io/api/v1`; added Production Vercel env vars `NEXT_PUBLIC_CARROT_SPACE_KEY`, `NEXT_PUBLIC_CARROT_LOCATION_ID`, `NEXT_PUBLIC_CARROT_API_URL`, and `CARROT_REVIEW_TOKEN`.
- Added `/kiosk` Carrot host page and `/kiosk/:path* -> /kiosk` rewrite; `/store/:path* -> /store` remains in place; both are force-dynamic/no-cache.
- Compliance/Sentinel initially returned NO-GO because `/store`, `/menu`, and `/kiosk` were public pre-launch and Carrot scripts loaded globally before age/review access. Fixed by moving Carrot scripts out of root layout and into `CarrotStore`, adding server-side review access checks on `/store` and `/kiosk`, and creating `/carrot-review` to set a 7-day HttpOnly review cookie.
- Legal pages `/privacy`, `/terms`, `/compliance` removed from pre-launch blockedRoutes and verified public 200 after deploy.
- Production deploy complete and aliased to `https://flushingcannabis.com`. Verified public `/store` and `/kiosk` return 307 to `/coming-soon` with no Carrot script/root in response; tokened `/store` and cookie-based `/kiosk` render Carrot root/script. Do not share the review URL publicly; send only to Carrot/Wei/Steven as needed.
- Remaining operational actions: Wei/team must rotate Carrot temporary password (`12345678`), configure categories once inventory arrives, enable online ordering, add staff accounts, and enable order notification texts.

## Flushing Cannabis Co. — Full-Site Preview Link Created
- Added `SITE_PREVIEW_TOKEN` to local + Vercel Production and created `/site-review?preview=...&target=/` route. It sets a 7-day HttpOnly `fcc_site_preview` cookie plus `fcc_carrot_review` so reviewers can browse the whole pre-launch site and open `/store`/`/kiosk` without exposing them publicly.
- Restored pre-launch public lockdown in `next.config.ts` using cookie-aware redirects: public `/`, `/about`, `/visit`, `/shop`, `/store`, `/kiosk` verified 307 → `/coming-soon`; preview-cookie sessions verified 200 for `/`, `/about`, `/visit`, `/shop`, `/store`, `/kiosk`; Carrot markers render only under preview/review access.
- Use canonical `https://flushingcannabis.com` for preview links. `https://www.flushingcannabis.com` showed an SSL hostname mismatch during curl verification even though Vercel alias lists it; avoid `www` until cert/DNS is corrected.

## Flushing Cannabis Co. — Wei Preview Email Tabled
- Steven asked to table the Wei email for now but confirmed it will be sent later. Draft saved to `drafts/flushing-cannabis-co/wei-site-carrot-preview-email.md` with private full-site preview link and Carrot shop review link. Do not send without Steven approval.
- Added follow-up to `TODO.md` and updated Mission Control Open Items.
