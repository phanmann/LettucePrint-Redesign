
## 10:00 AM — GMB API Basic Access follow-up
- Cron `check-gmb-api-access-approval` ran the read-only GBP API check for Google Cloud project `summer-surfer-491919-k3` / `525214453172`.
- Command: `python3 /Users/phanman/.openclaw/workspace/scripts/gmb_oauth.py check`
- Result: still blocked. Google returned HTTP 429 `RESOURCE_EXHAUSTED` with `quota_limit_value: 0` for `mybusinessaccountmanagement.googleapis.com/default_requests` (`DefaultRequestsPerMinutePerProject`).
- No OCC / Green Leaf NY account or location IDs pulled; no reviews pulled. Continue waiting for Google Basic Access approval.
> **Status update June 19, 2026:** Green Leaf NY is no longer a client. They are excluded from all active GBP workflows.
- Mission Control Open Items updated to reflect the May 13 quota-0 check.

## 10:40 AM — Flushing Cannabis Co. public launch
- Steven asked in #dispo-devops to remove the Coming Soon page / take the site live.
- Removed pre-launch `/coming-soon` redirects from `clients/flushing-cannabis-co/src/middleware.ts` and `next.config.ts`; deleted `src/app/coming-soon` routes; made `/store` and `/kiosk` public instead of Carrot review-token gated.
- Manual Sentinel pass: age gate + ShadowFooter remain; license number `OCM-RETL-25-000303` present; removed/softened riskier health/experience claims (`healing`, `therapeutic`, “staff tried it,” “works for you”).
- Gates passed: `npm run type-check`, `npm run build`, local route verification (`/`, `/about`, `/visit`, `/shop`, `/store`, `/kiosk` = 200; `/coming-soon` = 404).
- Deployed to Vercel production and aliased to `https://flushingcannabis.com`; live route verification passed (`/`, `/about`, `/visit`, `/shop`, `/store`, `/kiosk` = 200; `/coming-soon` = 404).
- Compliance/ops yellow flag: NY public license dataset returns Active license but `operational_status: Non-Operational` as of launch check; Mission Control updated. Keep an eye on online ordering readiness/categories/inventory/password rotation/notifications.
