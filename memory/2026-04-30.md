# Session Notes — Apr 30, 2026

## Flushing Cannabis Co. — Coming Soon + Launch Prep

### Coming Soon Page — Built from scratch
- Route: `/coming-soon`
- Standalone layout (initially had nested `<html>` bug → fixed by removing nested layout, using root layout)
- Age gate (`AgeGateModal`) included via root layout — applies automatically
- `SiteHeader` hidden on `/coming-soon` via `pathname === '/coming-soon'` check
- `SiteFooterConditional` wrapper hides SiteFooter on `/coming-soon`
- `ShadowFooter` (compliance) always shows

### Coming Soon Page — Design Iterations
1. Initial: dark scheme (black-sesame bg)
2. ADA contrast pass: red-bean accent → light-matcha, all opacity values lifted
3. Copy rewrite: "Something Good Is Coming" → "Flushing's First Legal Dispensary"
4. Bullet perks list added (first access, launch deals, drop alerts)
5. Light color scheme: warm cream `#f5f0ea` bg, stone text, red-bean + matcha accents
6. Animations added (all 6 via Stitch):
   - Staggered entrance via `AnimateIn` (logo→headline→copy→bullets→form→footer)
   - Red-bean bar wipe (new `barWipe` keyframe in tailwind.config.ts)
   - Tile-shimmer activated on background overlay
   - Button hover lift (`hover:-translate-y-0.5 hover:shadow-md`)
   - Success state slide-up (`animate-slide-up`)
   - Logo pulse (`animate-pulse-slow`)

### Light Color Scheme — Applied Site-Wide (via Stitch subagent)
- `cream: '#f5f0ea'` added to tailwind.config.ts
- All pages/components: `bg-black-sesame` → `bg-cream`, `text-milk-tea` → `text-stone-900`
- Subway tile overlay updated to dark-on-light (`rgba(0,0,0,0.04)`)
- SiteHeader, SiteFooter, ShadowFooter, AgeGateModal all updated
- All 13 page files updated
- Red-bean + matcha accents preserved throughout

### Waitlist API — `/api/waitlist`
- POST: stores to `data/waitlist.json`, duplicate check, optional Resend notification
- GET: protected by `WAITLIST_ADMIN_KEY` header (legacy, superseded by admin portal)
- Env vars: `WAITLIST_NOTIFY_EMAIL=steve@lettuceprint.com`, `WAITLIST_ADMIN_KEY` (set in Vercel)

### Pre-Launch Lockdown — middleware.ts
- All routes redirect 307 → `/coming-soon`
- Allowed: `/coming-soon`, `/api/waitlist`, `/api/verify-age`, `/admin`, `/api/admin`
- To disable: remove redirect block in middleware.ts and redeploy

### Admin Portal — `/admin`
- Password-protected login (scrypt hashed credentials)
- Session cookie: httpOnly, 8hr expiry, HMAC-signed
- Dashboard: total count stat, latest signup date, searchable table, CSV export, refresh
- API routes: `/api/admin/login`, `/api/admin/session` (GET=verify, DELETE=logout), `/api/admin/data`
- `ADMIN_SESSION_SECRET` set in Vercel env (encrypted)
- **Credentials — Wei Hu (client):** stored in `secrets/operational-credentials.json` → `flushing_cannabis.admin_portal_client_credentials` (raw password intentionally removed from memory)

### Domain — Live
- **Domain:** `flushingcannabis.com` + `www.flushingcannabis.com`
- **DNS:** A record `@` → `76.76.21.21`, CNAME `www` → `cname.vercel-dns.com` (set in Squarespace)
- **SSL:** Active (Vercel auto-provisioned)
- **Vercel project:** `prj_bngq3EihH0wHAWYDHsGTWGxiQmj9`
- DNS propagated same session — confirmed via curl (HTTP/2 200, 307 redirect working)
- GitHub auto-deploy NOT connected — use `npx vercel --prod --token "$VERCEL_TOKEN" --yes`

### Git State
- Latest commit: `e577f5f` — admin portal
- Branch: main
- Repo: github.com/phanmann/flushing-cannabis-co

---

## Flushing Cannabis Co. — UI Polish Session (Apr 30, evening)

### Visit Page — Store Hours Card Fixes
- Removed per-row `AnimateIn` wrappers from hours list — `translate-y-8` initial state was creating a large gap above Monday
- Heading `pb-3` → `pb-2`, hours div `mt-0` → `mt-1`
- Fine print spacing: `mt-5 pt-4` → `mt-12 pt-6` + `border-t border-stone-100` (proper visual separation from Sunday)
- Section top padding: `pt-6` → `pt-12`
- Hours row spacing: `py-1.5` (tried `py-3`, reverted back)

### Visit Page — Getting Here Card Fixes
- Same AnimateIn fix applied — removed per-row `AnimateIn` wrappers from 7 Train, Bus, Parking rows
- Rows now render flush to heading divider with no translate-y gap

### Global Font Size Bump
- `html { font-size: 17px }` added to globals.css (was browser default 16px)
- Custom type scale bumped: `body` 17px → 18px, `caption` 13px → 14px, `label` 11px → 12px
- Rationale: body text was hard to read, straining eyes

### Homepage — Shop By Category Section
- Added `bg-white pb-16` to section wrapper — cards were overflowing below the white section boundary

### Homepage — Neighborhood Strip Copy Update
- Body copy updated to Steven's new version: "Flushing is the kind of neighborhood you can't fake your way into..."
- "136-15 Roosevelt Ave" → "Roosevelt Ave" (store number removed from address line)

### NeighborhoodStrip — Stitch Redesign (APPROVED + DEPLOYED)
- **Analysis:** flat hierarchy, invisible cards (same bg as section), gap-px hack, no brand color in grid, labels shouting in wrong register, no visual anchor
- **Changes deployed:**
  - Cards: `bg-white/50 rounded-sm shadow-sm border-l-4 border-matcha` (lifted off background, matcha left accent)
  - Faint ordinal numbers (01–04) in `text-[#9ebd98]/25` as background anchors
  - Labels: `text-base` sentence-case (was `text-sm uppercase tracking-wider`)
  - Grid: `gap-4` between cards (replaced gap-px/bg-stone-200 hack)
  - Transit callout: `bg-white/80 rounded-sm shadow-sm` to match cards
  - AnimateIn kept at card level with stagger, not per-row

---

## Flushing Cannabis Co. — Hero Video + Nav + Icons Session (May 1 2026, ~midnight)

### Hero Section — Video Background
- Replaced static `flushing-mainstreet.jpg` with Flushing 7-train street video (`flushing-hero.mp4`) — autoplay, muted, loop, playsInline
- Video saved to `/public/flushing-hero.mp4`
- Overlay: dark `rgba(20,10,10,0.55)` (tried cream 80%/85%, reverted to dark)
- Hero expanded to `min-h-screen` (was `min-h-[70vh]`), `pt-20` removed — video bleeds behind navbar
- Hero text for dark bg: headline white, eyebrow `text-light-matcha font-bold drop-shadow-sm`, body `text-white text-lg drop-shadow-sm`
- Secondary CTA: white border/text

### Navbar — Transparent + Scroll Transition
- `bg-transparent` at top, `bg-[#f5f0ea]/95 backdrop-blur-md` on scroll
- Nav links + wordmark: white when transparent, stone-900 when scrolled
- Active underline: white over hero, red-bean when scrolled
- Logo: icon `w-8→w-10`, wordmark `text-sm→text-base`
- Nav links: `text-xs→text-sm`
- Shop Now: `text-sm px-6 py-2.5` + red-bean glow shadow + `hover:scale-[1.03]`
- Nav hover: bold text + color-aware underline

### Category Cards — SVG Icons
- Created `/src/components/icons/CategoryIcons.tsx` — 5 React SVG components
- Mapping: flower→FlowerIcon, vapes→VapeIcon, edibles→EdiblesIcon, pre-rolls→PreRollIcon, concentrates→ConcentratesIcon
- Icons: `w-10 h-10`, card textColor, 70% opacity → 90% on hover, top-left of each card
- SVGs provided by Steven
