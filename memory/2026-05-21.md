# Memory — 2026-05-21

## Memory System Hardening
- Steven asked to update Athena/OpenClaw memory so it is safer and more effective for the system and agents.
- Updated `AGENTS.md` with a formal Memory Architecture Policy:
  - layered memory: core, project, episodic, procedural, sensitive
  - mandatory memory_search/read pattern for prior-work questions
  - volatile facts are hints and must be verified live
  - subagents are read-mostly by default
  - raw secrets are forbidden in broad searchable memory
  - important memories should include source pointers when practical
- Created `secrets/operational-credentials.json` with chmod 600 as the reference location for operational credentials previously exposed in broad memory/docs.
- Redacted obvious raw secrets from `MEMORY.md`, `TOOLS.md`, and historical memory docs, replacing them with references to `secrets/operational-credentials.json`, `secrets/supabase.json`, or provider/env vaults.
- Created `memory/memory-eval-set.md` with 25 regression prompts covering identity, memory policy, Lettuce Print operations, project context, and credential-handling safety.
- Updated Mission Control infrastructure/open-items card to reflect the completed memory hardening.

## LP Website Progress Saved
- Saved Lettuce Print website progress in `lp-redesign/agents/lp-website/session-2026-05-21.md`.
- Current product-code HEAD: `2b4ffaf` (`Update portfolio project to County Road Cannabis`), with local and origin/main matching before docs save.
- Portfolio work now includes redesigned details, project galleries, organized image folders, seven-project curation, Rolling Green Cannabis, and County Road Cannabis.
- QA: `npm run build` passes; `npm run lint` still fails on existing lint blockers (unescaped entities, React 19 set-state-in-effect, unused vars).
- Next: lint cleanup → build → Stripe E2E → Resend contact verification → domain + GA/SEO.

## Flushing Cannabis Co. — GSC duplicate canonical warning
- Steven shared a Google Search Console warning for Flushing: “Duplicate without user-selected canonical.”
- Triage: site had `metadataBase` but indexable pages did not explicitly set `alternates.canonical`; Carrot `/store/*` rewrites can also produce duplicate URLs that should canonicalize back to `/store`.
- Added explicit canonical metadata locally for `/`, `/shop`, `/store`, `/about`, `/visit`, `/contact`, `/compliance`, and `/dispensary/flushing-queens`.
- `npm run type-check && npm run build` passed in `clients/flushing-cannabis-co` on May 21, 2026. Production deploy is pending Steven approval; after deploy, validate fix in Google Search Console.

- Steven approved deploy. Deployed to Vercel production and aliased to https://flushingcannabis.com. Live verification passed: `/`, `/shop`, `/store`, `/about`, `/visit`, `/contact`, `/compliance`, `/dispensary/flushing-queens` all return 200 with self-canonicals; `/store/category/flower` returns 200 and canonicalizes to `https://flushingcannabis.com/store`. Next step: click Validate Fix in Google Search Console.
