
## Flushing Cannabis Co. — Carrot image loading fix
- Diagnosed Carrot shopping images not loading: not a WordPress/Kinsta cache issue for this site (Flushing is Next/Vercel). The real blocker was CSP rejecting Carrot's DigitalOcean media hosts, e.g. `carrot-nevada-media.sfo2.cdn.digitaloceanspaces.com` and `carrot-static.ams3.cdn.digitaloceanspaces.com`.
- Updated `clients/flushing-cannabis-co/next.config.ts` to allow exact Carrot media/CDN/payment/search dependencies and strengthened no-store headers on `/store` + `/kiosk` (`Cache-Control`, `CDN-Cache-Control`, `Vercel-CDN-Cache-Control`). Added `fetchCache = 'force-no-store'` to store/kiosk pages.
- Sentinel/legal check returned GO conditional: no new NY OCM blocker as long as age restriction remains and CSP does not expose new public menu surfaces. Current site uses public launch mode with client-side age gate; OpenReplay remains blocked by CSP intentionally due analytics/PII caution.
- Verified `npm run type-check` ✅ and `npm run build` ✅, deployed to Vercel production and aliased to `https://flushingcannabis.com` ✅. Live `/store` headers show `no-store`, `cdn-cache-control: no-store`, updated CSP image hosts, and `x-vercel-cache: MISS`. Browser screenshot verification showed Carrot store/product/category UI and images visible. Commit in client repo: `6fb1ebc` (`fix: allow Carrot product media`).
- Mission Control updated: Flushing item now notes Carrot image-loading CSP/no-cache fix deployed May 15 and verified live.
