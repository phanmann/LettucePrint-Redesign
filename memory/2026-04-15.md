
## Session — April 15, 2026 (In-Office Setup Day)

### Completed Today
- **Janet Discord connected** — User ID `929872698610417694`, added to `#athena-ai` (channel `851595929257902100`), send messages permission confirmed, welcome message sent
- **Apple Notes MCP** — Installed `@ailenshen/apple-notes-mcp`, registered in OpenClaw MCP config. Read works. Write via `osascript` confirmed working directly. MCP import-sheet method unreliable; use `osascript` going forward.
- **Apple Reminders** — `remindctl` already installed, authorized to Full Access via `remindctl authorize`. Syncs to iPhone.
- **gog (Google Workspace CLI)** — OAuth credentials saved, `athena@lettuceprint.com` authenticated with Gmail, Calendar, Drive, Contacts, Docs, Sheets. Fully operational.
- **All For Signs** — Credentials saved to `secrets/allforsigns.json` (info@lettuceprint.com / HazyHaus420!). Browser login confirmed via `agent-browser`. Account shows "Hello, Janet".
- **agent-browser** — Installed via `brew install agent-browser`, Chrome for Testing 147.0.7727.56 downloaded. Browser automation fully operational.
- **Tailscale** — Connected on tailnet `lettuceprint.com` as `steven's-mac-mini-2` at `100.115.8.108`. Gateway updated to `bind=loopback`, `tailscale.mode=serve` (config set, phone pairing skipped per Steven's request).
- **Cloudflare Tunnel** — Attempted, set up `cloudflared` as system service with token. Used `trycloudflare.com` temporarily for Mission Control public access. Later killed per Steven's decision to use Tailscale instead.
- **Mission Control** — Local server at `http://localhost:8765` made permanent via LaunchAgent (`ai.lettuceprint.missioncontrol.plist`). Accessible via Tailscale at `http://100.115.8.108:8765`. Dashboard updated: Gmail, gog, All For Signs all marked ✅ Live.
- **Vertex AI** — `gcloud-cli` installed, service account activated, `cloudresourcemanager` and `aiplatform` APIs enabled. `google-cloud-aiplatform` Python SDK installed. Blocked on billing — Steven didn't have card. Pending.
- **Kimi K2** — API key `sk-0SGUdLt6rzYpVWKt6plSAwLZ0lb5NVes48YWXGZLFCYXj650` confirmed working. Saved to `secrets/kimi.json`. Also registered as `models.providers.kimi` in openclaw.json.
- **OpenRouter** — API key `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` configured as provider. Routes: Athena → `claude-sonnet-4-6`, heartbeat → `gemini-2.5-flash-lite`, subagents default → `kimi-k2`.
- **Heartbeat optimized** — Light context ON, active hours 8am–7pm ET, cheap Gemini Flash model. No more 3am token burns.
- **Rosetta 2** — Installed successfully.

### Pending
- **Gmail domain-wide delegation** — Scopes added in Google Admin Console. Still returning 401 as of session end — needs retest after propagation (can take up to 60 min).
- **Vertex AI billing** — Steven adding credit card. Will test Imagen 4 + Veo 2 once billing active.
- **Queenie Quotes spawn call** — Needs explicit `model: "openrouter/anthropic/claude-opus-4-5"` added to her spawn script.
- **Apple Notes write via MCP** — MCP import-sheet method still flaky. Use osascript directly.

### Removed from To-Do
- Custom client portal (removed by Steven)
- Ollama / Gemma 4 local model setup (removed by Steven)

### Config State (as of session end)
- Athena model: `openrouter/anthropic/claude-sonnet-4-6`
- Heartbeat model: `openrouter/google/gemini-2.5-flash-lite-preview-06-17`
- Subagents default: `openrouter/moonshotai/kimi-k2`
- OpenRouter base URL: `https://openrouter.ai/api/v1`
- Kimi direct: `https://api.moonshot.ai/v1`
- Gateway: loopback + Tailscale serve mode, port 18789
- Tailscale IP: `100.115.8.108`

### Secrets saved today
- `secrets/allforsigns.json` — All For Signs login
- `secrets/kimi.json` — Kimi K2 API key
- `secrets/gog-client-secret.json` — Google OAuth client secret (already existed)

## Standing Rule — Mission Control Auto-Update
Whenever something changes (integration goes live, open item resolved, new blocker, config updated), update `/Users/phanman/.openclaw/workspace/dashboard/index.html` automatically. Do NOT wait for Steven to ask. Mission Control should always reflect current reality.
