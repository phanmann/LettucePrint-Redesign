# 2026-05-28

## Flushing Cannabis — Surfside CSP Collector

- Steven relayed a provider request in `#dispo-devops` to unblock `https://col.surfside.io/` for Flushing Cannabis.
- Updated `clients/flushing-cannabis-co/next.config.ts` CSP to allow `https://col.surfside.io` in `script-src`, `img-src`, and `connect-src`.
- Ran `npm run build` successfully.
- Ran Sentinel-style compliance sanity check: no customer-facing cannabis claims/copy changed; update is technical CSP allowlist only. Existing Surfside privacy/measurement disclosure remains in place from prior Surfside work.
- Deployed production with Vercel. Deployment `dpl_4kSdWAY2Bjo19Gtt3hdp4g4FiU6f`; alias `https://flushingcannabis.com`.
- Verified live `/store` CSP headers on both apex and `www` include `https://col.surfside.io`.
- Updated Mission Control open item for Flushing Surfside CSP.

## OpenClaw Model Routing — Kimi K2.6

- Steven said "we should have the Kimi model setup."
- Confirmed bundled OpenClaw `moonshot` and `kimi` plugins were installed/enabled, and `MOONSHOT_API_KEY` was present in the runtime environment.
- Found active `~/.openclaw/openclaw.json` had `moonshot/kimi-k2.6` referenced as the main subagent default, but the configured Moonshot provider only exposed `kimi-k2.5`.
- Updated `~/.openclaw/openclaw.json` to add `moonshot/kimi-k2.6` to the Moonshot provider and alias it as `Kimi`; kept `kimi-k2.5` available as `Kimi 2.5`.
- `openclaw config validate` passed.
- `openclaw models list --status-plain` now shows `moonshot/kimi-k2.6` as configured/authenticated with 256k context and text+image input.
- Live test passed: `openclaw infer model run --model moonshot/kimi-k2.6 --prompt 'Reply with exactly: Kimi online' --json` returned `Kimi online`.
- Updated Mission Control `dashboard/index.html` to show Kimi K2.6 routing as live and update the AI layer from Kimi K2.5 to Kimi K2.6.
